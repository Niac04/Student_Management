# Copy to .env and fill values before running
PORT=5000

# MySQL / Sequelize settings
DB_HOST=localhost
DB_NAME=student_management
DB_USER=root
DB_PASSWORD=yourpassword

# Optional: simple API key for protecting endpoints
API_KEY=

# Email (used by src/utils/emailService.js) - optional for local dev
EMAIL_USER=you@example.com
EMAIL_PASS=your-email-password
